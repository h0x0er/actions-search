{
  "name": "actions-search",
  "version": "0.0.4",
  "description": "Browser extension to search/filter workflows in the GitHub Actions sidebar",
  "scripts": {
    "dev": "wxt",
    "dev:firefox": "wxt -b firefox",
    "build": "wxt build",
    "build:chrome": "wxt build -b chrome",
    "build:firefox": "wxt build -b firefox",
    "build:edge": "wxt build -b edge",
    "build:all": "npm run build:chrome && npm run build:firefox && npm run build:edge",
    "zip": "wxt zip",
    "zip:chrome": "wxt zip -b chrome",
    "zip:firefox": "wxt zip -b firefox",
    "zip:edge": "wxt zip -b edge",
    "zip:all": "npm run zip:chrome && npm run zip:firefox && npm run zip:edge"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "wxt": "^0.20.26"
  }
}
